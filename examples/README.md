# Ciao examples

These deliberately small projects exercise detection and local development:

- `rust-app`: Cargo service
- `go-app`: Go service
- `bun-app`: Bun service with a build script
- `static-site`: static output served without an application runtime

Run from an example directory with `ciao inspect .`, then `ciao run` or deploy
it to a test host.
